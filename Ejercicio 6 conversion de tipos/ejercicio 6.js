
// Ejercicio 6

let hello = 'Ciao';         
let age = 18;               
let isGraduated = false;    

// Convertir hello a tipo de datos booleano
hello = Boolean(hello);

// Convertir age a tipo de datos cadena (string)
age = String(age);

// Convertir isGraduated a tipo de datos numérico (number)
isGraduated = Number(isGraduated);
