// Ejercicio 20

// Crear la variable primitive y asignarle un valor
let primitive = "hola!";

// Comprobar el tipo de la variable y mostrarlo en la consola
if (typeof primitive === "number") {
  console.log("La variable primitive es de tipo numérico.");
} else if (typeof primitive === "string") {
  console.log("La variable primitive es de tipo string.");
} else if (typeof primitive === "boolean") {
  console.log("La variable primitive es de tipo booleano.");
} else {
  console.log("La variable primitive tiene un tipo no reconocido.");
}
