
// Ejercicio 7

let hello = 'Ciao';     // number
let age = 18;       // boolean
let isGraduated = false;     // string

// conversión de tipos

hello = Number(hello)
age = Boolean(age)
isGraduated = String(isGraduated)

// Imprimir en consola

console.log(hello)
console.log(age)
console.log(isGraduated)

// A la hora de ejecutar en consola la variable hello, esta nos dice NaN, que quiere decir que no es un valor numérico.